#include <stdio.h>

int main() {
    float num;
    
    // Taking float input
    scanf("%f", &num);
    
    // Printing the float with two decimal places
    printf("You entered: %.2f\n", num);
    
    return 0;
}