#include <stdio.h>

int main() {
    float num;

    // Take input using scanf
    scanf("%f", &num);

    // Print the entered float with 2 decimal places
    printf("You entered: %.2f\n", num);

    return 0;
}